<!-- TODO — planned work: features, chores, improvements. IDs are TODO-1, TODO-2, … never reused.
     NOT the bug log (defects go there, as #N) and NOT the plan log (a TODO item that gets planned
     links to its plan). TRIAGE on entry: place every new item in Now / Next / Later — don't let
     items pile up unsorted. Reconcile trigger-based: any session that opens/closes/moves/descopes
     an item updates this file (it's a row in the write-handoff pre-handoff sweep).
     ONE file with priority sections beats multiple files (a split means every triage touches two
     files). Split out a lane ONLY when it has its own audience/cadence — e.g. a TODO-QA.md for
     test debt reviewed on its own rhythm. -->

# TODO

Item format: `- [ ] TODO-N — one line (links: bug #N / plan / initiative), and for Later items the
REVISIT TRIGGER — "revisit when X" beats "someday".`

## Now — current focus (this session or the next)

_(empty)_

## Next — queued behind Now

_(empty)_

## Later — parked, each with its revisit trigger

_(empty)_

## Done — moved here with date + cross-ref to the implementation-log entry

_(empty)_
