<!-- PLAN LOG — every plan (even one-line changes), APPEND AT EOF (newest last).
     Entry template lives in the plan-first skill. Status lifecycle:
     ⏳ PLAN — awaiting decisions + "go"  →  🟢 APPROVED <date>  →  ✅ RESOLVED <date>
     Flip to APPROVED the moment the user gives the go; flip to RESOLVED only after
     the user verifies the change in the real environment. -->

# Plan Log

_No plans yet — the first plan is appended at the end of this file._
