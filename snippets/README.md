# Snippets

Reusable code bits and commands worth keeping.

| Folder | What's in it |
|---|---|
| [`hubspot-readonly/`](hubspot-readonly/) | Read-only CLI helpers for grounding against a live HubSpot portal — find-property (by label), get-record, search-records, list-associations (full cursor walk), find-form, orphan-audit. Zero dependencies, token via env or gitignored file, never writes. |
